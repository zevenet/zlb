{
	"method": "PUT",
	"url": "/farms/<farmname>/service/<servicename>/replaceresponsetheader/<id>",
	"action": "add",
	"description": "Add rules to rewrite the backend headers.",
	"params": {
		"header": {
			"non_blank": "true",
			"is_regex": "true",
			"negated_regex": "$newline"
		},
		"match": {
			"non_blank": "true",
			"is_regex": "true",
			"negated_regex": "$newline"
		},
		"replace": {
			"negated_regex": "$newline"
		}
	}
}
