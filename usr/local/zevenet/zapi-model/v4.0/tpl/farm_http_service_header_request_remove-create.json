{
	"method": "POST",
	"url": "/farms/<farmname>/service/<servicename>/removerequestheader",
	"action": "add",
	"description": "Remove certain headers from the client requests.",
	"params": {
		"pattern": {
			"non_blank": "true",
			"required": "true",
			"negated_regex": "$newline"
		}
	}
}
