{
	"method": "POST",
	"url": "/aws/ssh",
	"action": "set",
	"description": "Modify the SSH configuration for AWS.",
	"params": {
		"remote_ip": {
			"description": "",
			"non_blank": "true",
			"regex": "$ssh_listen"
		}
	}
}
