{
	"method": "POST",
	"url": "/farms/<farmname>/services/<id>/actions",
	"action": "move",
	"description": "This action move the given a service Up or Down in the Service List, this option is useful if it is required to change the priority order, note that services are evaluated in the same order that is shown. this action restarts the farm automatically.",
	"params": {
		"position": {
			"regex": "$integer",
			"non_blank": "true",
			"required": "true"
		}
	}
}
