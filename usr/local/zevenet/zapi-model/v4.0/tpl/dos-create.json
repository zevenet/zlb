{
	"method": "POST",
	"url": "/ipds/dos",
	"action": "create",
	"description": "Create a DoS object to use it in as farms as it is necessary.",
	"params": {
		"name": {
			"description": "It is the object rule unique identifier.",
			"regex": "$dos_name",
			"non_blank": "true",
			"required": "true",
			"exceptions": [
				"rules",
				"0"
			]
		},
		"rule": {
			"description": "It is the type rule identifier. The options are: limitconns, limitsec, limitrst or bogustcpflags.",
			"non_blank": "true",
			"required": "true",
			"values": [
				"limitconns",
				"limitsec",
				"bogustcpflags",
				"limitrst"
			]
		}
	}
}
