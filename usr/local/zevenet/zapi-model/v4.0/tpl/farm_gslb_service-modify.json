{
	"method": "PUT",
	"url": "/farms/<farmname>/services/<id>",
	"action": "set",
	"description": "Modify the parameters of a GSLB service.",
	"params": {
		"deftcpport": {
			"regex": "$port",
			"non_blank": "true",
			"required": "true",
			"exceptions": [
				"0"
			]
		}
	}
}
