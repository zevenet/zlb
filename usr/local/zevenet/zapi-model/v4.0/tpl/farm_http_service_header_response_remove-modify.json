{
	"method": "POST",
	"url": "/farms/<farmname>/service/<servicename>/removeresponseheader",
	"action": "add",
	"description": "Remove certain headers from the backend responses.",
	"params": {
		"pattern": {
			"non_blank": "true",
			"required": "true",
			"negated_regex": "$newline"
		}
	}
}
