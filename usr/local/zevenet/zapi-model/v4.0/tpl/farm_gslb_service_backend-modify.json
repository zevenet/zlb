{
	"method": "PUT",
	"url": "/farms/<farmname>/services/<id>/backends/<id>",
	"action": "set",
	"description": "Modify the parameters of a backend GSLB service.",
	"params": {
		"ip": {
			"regex": "$ip_addr",
			"non_blank": "true",
			"required": "true"
		}
	}
}
