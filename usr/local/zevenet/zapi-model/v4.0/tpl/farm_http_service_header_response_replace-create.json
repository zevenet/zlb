{
	"method": "POST",
	"url": "/farms/<farmname>/service/<servicename>/replaceresponsetheader",
	"action": "add",
	"description": "Add rules to rewrite the backend headers.",
	"params": {
		"header": {
			"non_blank": "true",
			"required": "true",
			"is_regex": "true",
			"negated_regex": "$newline"
		},
		"match": {
			"non_blank": "true",
			"required": "true",
			"is_regex": "true",
			"negated_regex": "$newline"
		},
		"replace": {
			"required": "true",
			"negated_regex": "$newline"
		}
	}
}
