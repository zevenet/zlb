{
	"method": "PUT",
	"url": "/farms/<farmname>/services/<service>/addresponseheader/<id>",
	"action": "modify",
	"description": "Add a header in the backend response.",
	"params": {
		"header": {
			"non_blank": "true",
			"required": "true",
			"negated_regex": "$newline"
		}
	}
}
