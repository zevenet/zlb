{
	"method": "POST",
	"url": "/ipds/waf",
	"action": "create",
	"description": "Create a WAF ruleset to be used to farms. It is created empty without any rule and all it is disabled by default",
	"params": {
		"name": {
			"description": "WAF ruleset name. It is used as the unique identifier.",
			"regex": "$waf_set_name",
			"non_blank": "true",
			"required": "true",
			"exceptions": [
				"options",
				"files",
				"0"
			]
		},
		"copy_from": {
			"description": "It is a WAF ruleset name, if this parameter is sent the new set will be a copy from this.",
			"regex": "$waf_set_name",
			"non_blank": "true"
		}
	}
}
