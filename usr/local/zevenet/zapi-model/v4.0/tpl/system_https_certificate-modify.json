{
	"description": "Modify the HTTPS service certificate.",
	"method": "POST",
	"url": "/system/https/certificate",
	"action": "set",
	"params": {
		"certificate": {
			"description": "Certificate name.",
			"regex": "$cert_pem",
			"non_blank": "true",
			"required": "true"
		}
	}
}
