{
	"method": "PUT",
	"url": "/ipds/dos/<name>",
	"action": "set",
	"description": "Modify a limit reset DoS rule. name is the DoS rule unique identifier.",
	"params": {
		"limit": {
			"description": "It is the maximum number of reset requests per second for a source.",
			"regex": "$natural_num",
			"non_blank": "true"
		},
		"limit_burst": {
			"description": "It is an allowed peak of reset request. A burst unit regenerates when “limit” seconds will pass.",
			"regex": "$integer",
			"non_blank": "true"
		}
	}
}
