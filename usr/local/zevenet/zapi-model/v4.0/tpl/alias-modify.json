{
	"method": "PUT",
	"url": "/aliases/<backends|interfaces>/<id>",
	"action": "set",
	"description": "If the object has an alias, it will be replaced, else an alias will be created for the object.",
	"params": {
		"alias": {
			"regex": "$alias_name",
			"non_blank": "true",
			"required": "true",
			"exceptions": [
				"0"
			]
		}
	}
}
