{
   "action" : "set",
   "description" : "Modify the configuration of a user. name is the user name and it is a unique identifier.",
   "method" : "PUT",
   "params" : {
      "newpassword" : {
         "description" : "New user password to access through the WebGUI.",
         "format_msg" : "must contain at least a letter and a number and a minimum length of 8 characters.",
         "non_blank" : "true",
         "regex" : "(?^:(?=.*[0-9])(?=.*[a-zA-Z]).{8,512})"
      },
      "webgui_permissions" : {
         "description" : "The possible values are: true, to enable the access to the load balancer through the WebGUI; or false, to not allow accessing through the WebGUI.",
         "non_blank" : "true",
         "values" : [
            "false",
            "true"
         ]
      },
      "zapi_permissions" : {
         "description" : "The possible values are: true, to enable the access to the load balancer through the ZAPI; or false, to not allow accessing through the ZAPI.",
         "non_blank" : "true",
         "values" : [
            "false",
            "true"
         ]
      },
      "zapikey" : {
         "description" : "It is the group name where the user is. The user can be only in one group.",
         "regex" : "(?^:[a-zA-Z0-9]+)"
      }
   },
   "url" : "/rbac/users/<name>"
}
