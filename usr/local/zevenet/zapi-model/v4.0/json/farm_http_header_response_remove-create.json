{
   "action" : "add",
   "description" : "Remove certain headers from the backend response.",
   "method" : "POST",
   "params" : {
      "pattern" : {
         "is_regex" : "true",
         "negated_regex" : "(?^:\\n)",
         "non_blank" : "true",
         "required" : "true"
      }
   },
   "url" : "/farms/<farmname>/removeresponseheader"
}
