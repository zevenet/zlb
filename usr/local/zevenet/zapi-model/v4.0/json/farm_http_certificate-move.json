{
   "action" : "move",
   "description" : "This feature allows moving the certificates to alter the order that they will be checked.",
   "method" : "POST",
   "params" : {
      "position" : {
         "non_blank" : "true",
         "regex" : "(?^:\\d+)",
         "required" : "true"
      }
   },
   "url" : "/farms/<farmname>/certificates/<file>/actions"
}
