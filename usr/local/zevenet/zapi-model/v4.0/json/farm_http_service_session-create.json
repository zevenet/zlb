{
   "action" : "add",
   "description" : "It creates a static session with a backend in a http farm.",
   "method" : "POST",
   "params" : {
      "id" : {
         "dyn_values" : "true",
         "non_blank" : "true",
         "regex" : "(?^:\\d+)",
         "required" : "true"
      },
      "session" : {
         "non_blank" : "true",
         "required" : "true"
      }
   },
   "url" : "/farms/<farmname>/services/<servicename>/sessions"
}
