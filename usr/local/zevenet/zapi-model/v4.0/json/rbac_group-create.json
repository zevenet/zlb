{
   "action" : "set",
   "description" : "Create a new group where users, resources and roles are related.",
   "method" : "POST",
   "params" : {
      "name" : {
         "non_blank" : "true",
         "regex" : "(?^:[\\w-]+)",
         "required" : "true"
      }
   },
   "url" : "/rbac/groups"
}
