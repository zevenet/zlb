{
   "action" : "set",
   "description" : "Modify a domain or the user domain list by other that is sent as parameter.",
   "method" : "PUT",
   "params" : {
      "domain" : {
         "description" : "It is a domain that it replaces the domain sent in the URI.",
         "non_blank" : "true",
         "regex" : "(?^:[\\w\\.\\-]+)",
         "required" : "true"
      }
   },
   "url" : "/ipds/rbl/domains/<id>"
}
