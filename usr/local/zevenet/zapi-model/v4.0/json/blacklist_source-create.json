{
   "action" : "add",
   "description" : "Push a new source to a blacklist. Only available for local lists and non-preloaded lists. name is the blacklist unique identifier.",
   "method" : "POST",
   "params" : {
      "source" : {
         "description" : "IP address or network segment that the list checks.",
         "non_blank" : "true",
         "ref" : "array|none",
         "required" : "true"
      }
   },
   "url" : "/ipds/blacklists/<name>/sources"
}
