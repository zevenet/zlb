{
   "action" : "status",
   "description" : "Apply a action to a dos rule. name is the dos rule unique identifier.",
   "method" : "POST",
   "params" : {
      "action" : {
         "description" : "Action desired to apply to a DoS rule. The available values are: start, it actives the DoS rule in all farms where it is applied; stop, it disables the DoS for all farms where the DoS rule is applied, the traffic won’t be blocked or allowed.",
         "non_blank" : "true",
         "required" : "true",
         "values" : [
            "start",
            "stop",
            "restart"
         ]
      }
   },
   "url" : "/ipds/dos/<name>/actions"
}
