{
   "action" : "set",
   "description" : "It modifies the backend alerts.",
   "method" : "POST",
   "params" : {
      "avoidflappingtime" : {
         "depend_on" : "alert==backends",
         "description" : "Sometimes backends are available or not in a short period, this field allows to configure a time to avoid notifications message while the backend is changing status, once this time concludes, if the backend status it is different to start, then the notification is sent.",
         "non_blank" : "true",
         "regex" : "(?^:[1-9]\\d*)"
      },
      "prefix" : {
         "depend_on" : "alert==cluster || alert==backends",
         "description" : "It is a prefix for the email subject, that are sent by the system for this kind of alert.",
         "regex" : "[?[\\w-\\s]+]?"
      }
   },
   "url" : "/system/notifications/alerts/backends"
}
